# Core pipeline — needed to fetch, parse and analyse the reports.
requests>=2.32
pypdf>=6.0

# Optional. Only needed to regenerate the charts in report/.
# matplotlib>=3.9

# Optional. Only needed for scripts/llm_extract.py, the LLM second pass.
# anthropic>=1.0
